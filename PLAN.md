# 实施计划

本计划供后续 Codex Goal 执行。它把项目拆成可验证的阶段，每个阶段都要留下可复用的文件产物并满足退出条件。产品边界以 `CONSTRAINTS.md` 为准。

## 执行原则

- 按阶段推进；允许为减少返工进行小范围并行，但不得跳过阶段退出条件。
- 先建立来源清楚的数据，再设计和实现页面；不以未经核实的内容填满界面。
- 所有页面围绕游客的“发现、比较、决定、抵达、游览”流程组织。
- 三个详情页共享稳定的交互基础，同时允许根据博物馆、革命旧址群和山地景区的差异采用不同的信息表现。
- 每完成一个阶段，更新本文件的状态和实际产物；新问题记录为待决事项，能配置化处理的先保留配置入口。
- Goal 开始前，所有阶段均保持未开始状态。

## 阶段 0：建立工程与记录基线

**状态：已完成**

目标是让后续研究、编码和部署可追踪、可恢复。

主要工作：

- 检查并初始化适合静态网站的版本管理与忽略规则；
- 采用不含错误地域名称的内部项目标识；
- 建立研究资料、来源记录、图片台账、待决事项和页面数据的目录规范；
- 确定本地运行、检查、构建和 Cloudflare 部署命令；
- 记录实际选择的前端技术方案。默认候选为 Vite、React 和 TypeScript，但应在检查环境后再落定，不把候选方案当成既成事实。

退出条件：

- 新成员或后续 Goal 能从仓库文档理解目录用途和常用命令；
- 项目名称、包名和页面内容不再继承 `Henan` 误命名；
- 研究来源和图片授权有固定记录位置。

实际产物：

- `package.json`、`pnpm-lock.yaml`、Vite/TypeScript/ESLint 配置和最小可构建入口；
- `.git/` 与 `.gitignore`；
- `README.md`、`docs/TECHNICAL_DECISIONS.md`、`OPEN_QUESTIONS.md`；
- `sources/reference.txt`、`sources/images/IMAGE_LEDGER.csv` 和 `sources/research/`；
- 2026-08-27 已通过 `pnpm lint`、`pnpm typecheck` 和 `pnpm build`。

## 阶段 1：资料与图片研究

**状态：已完成**

目标是为三个目的地建立足以支撑页面和地图的可靠资料库。

主要工作：

- 分析参考网站的信息组织和交互方式，只记录可借鉴的原则，不复制实现；
- 逐一调研河北博物院红色展厅、西柏坡革命旧址和狼牙山；
- 收集并核验位置、核心看点、内部景点、开放与预约、抵达方式、内部交通、建议用时、游览强度和行前准备；
- 为易变化信息记录来源、发布日期或核验日期；
- 搜集候选网络图片，建立包含来源、作者或机构、使用条件、用途和本地文件名的台账；
- 对无法核实的信息和缺失图片明确标记，不用推测填补。

退出条件：

- 三个目的地均有结构一致的基础资料，足以支持比较和行程规划；
- 每项关键事实可以追溯到来源；
- 正式图片均可说明使用依据，缺图项均有占位策略；
- 内部景点和路线信息已达到可以绘制可视化地图的程度，或明确记录了不能达到的原因。

实际产物：

- `sources/reference.txt`：按事实用途记录的来源索引；
- `sources/research/reference-site/interaction-notes.md`：参考网站交互观察与原创边界；
- `sources/research/hebei-museum/official-guide.md`、`xibaipo/official-guide.md`、`langya-mountain/official-guide.md`：三个目的地结构化研究笔记；
- `sources/research/maps/data-source.md` 与 `public/data/hebei.geojson`：河北位置示意数据及适用限制；
- `sources/images/IMAGE_LEDGER.csv`：官网候选图、研究附件和发布占位策略。官网候选图未发现公开复用许可，发布版暂不使用。

## 阶段 2：产品结构与交互原型

**状态：已完成**

目标是在写完整页面前验证游客能否顺利完成核心任务。

主要工作：

- 定义“发现目的地、比较选择、查看详情、规划抵达、规划游览”的用户流程；
- 确定首页河北地图、目的地摘要、三地比较和详情页之间的导航关系；
- 设计景点点击、地图定位、路线联动、图片展示和侧栏信息的交互状态；
- 分别设计博物馆室内展陈、旧址群和山地路线适合的详情页表达；
- 完成桌面端与移动端低保真原型，包含加载、缺图、无数据和操作反馈状态；
- 确定视觉方向，避免把页面做成活动汇报、传统新闻专题或纯图文宣传册。

退出条件：

- 不阅读说明文字，用户也能识别三个目的地并进入详情；
- 用户能在原型中找到抵达方式、建议用时、内部路线和准备事项；
- 手机端的地图、弹层和侧栏有明确且不重叠的布局方案；
- 原型不依赖尚未获得的非图片类占位内容。

实际产物：

- `docs/PRODUCT_SPEC.md`：用户流程、信息架构、交互、状态和视觉方向；
- `docs/WIREFRAMES.md`：首页与详情页的桌面/移动线框，以及三类目的地地图差异；
- 图片缺失使用项目自有占位状态，地图、路线和交通信息均由已核验数据实际绘制。

## 阶段 3：数据模型与前端基础

**状态：已完成**

目标是搭建可维护的静态站骨架，让内容与页面实现解耦。

主要工作：

- 建立目的地、内部景点、路线、交通、提示、图片和来源的数据结构；
- 让易变化信息、公开品牌和外部服务配置可集中修改；
- 建立响应式布局、字体、颜色、间距、图标、按钮、弹层、抽屉和占位图规范；
- 完成路由、静态资源处理、基础错误状态和构建配置；
- 配置必要的代码质量与自动检查命令。

退出条件：

- 三个目的地可以由数据驱动生成，不需要复制整页代码；
- 替换图片、交通信息或景点顺序无需修改核心组件；
- 静态构建成功，并能在本地以接近 Cloudflare 的路径规则访问。

实际产物：

- `src/types/tourism.ts` 与 `src/data/destinations.ts`：三地、点位、路线、交通、准备事项和来源的统一配置；
- `src/components/`：导航、图片占位、地图、比较、路线、交通和来源等共用组件；
- `src/App.tsx`、`src/styles.css` 与 `public/_redirects`：路由、响应式视觉系统和静态路径回退；
- 2026-08-27 通过 `pnpm lint`、`pnpm typecheck` 和 `pnpm build`。

## 阶段 4：首页与河北总览

**状态：已完成**

目标是让游客在首屏理解三个目的地在哪里、各自适合什么行程，并能继续探索。

主要工作：

- 实现河北地图、三个目的地标记和可访问的交互热点；
- 实现地图与目的地摘要的联动；
- 展示主题特色、相对位置、建议用时、游览强度和主要抵达方式；
- 提供服务于游客选择的三地比较入口；
- 完成键盘、触控、缩放和小屏布局处理。

退出条件：

- 地图是实际可交互的信息界面，不是装饰背景；
- 三个目的地的位置关系和核心差异无需阅读长文即可理解；
- 所有主要操作在桌面和手机上可达、可见且有反馈。

实际产物：

- `src/components/HebeiMap.tsx` 使用本地河北 GeoJSON 绘制 11 个地市边界，并规范源数据多边形方向；
- 三个真实位置按钮与当前目的地摘要联动，地图失败时保留三地选择入口；
- `src/components/ComparisonTable.tsx` 从用时、强度、环境、抵达和适合人群比较三地；
- 浏览器在 1440×900 和 390×844 视口验证地图非空、三个标记可选且无页面横向溢出。

## 阶段 5：三个目的地详情体验

**状态：已完成**

目标是让游客完成从“感兴趣”到“知道怎样去、怎样游览”的决策。

主要工作：

- 实现共用详情页框架和三个目的地各自适合的地图或路线表达；
- 实现内部景点点击、位置高亮、图片、介绍、前往路线和交通方式联动；
- 实现抵达方式、建议游览顺序、预计耗时、准备事项和适合人群；
- 对网络图片缺失或加载失败提供统一占位状态；
- 对可能变化的信息显示来源或核验日期，并提供进一步核实入口。

退出条件：

- 三个详情页都能完整回答 `CONSTRAINTS.md` 第 3 节的问题；
- 景点、路线和侧栏之间存在真实联动，不是三块互不相关的静态内容；
- 三页共享一致的操作逻辑，但没有为了套模板而抹平目的地差异；
- 除图片外，没有影响理解和行动的空占位内容。

实际产物：

- `src/pages/DestinationPage.tsx` 与 `src/components/RouteExplorer.tsx`：共用详情框架及楼层、院落、山地三种路线表达；
- 地图点位、路线步骤、选走支线、图片占位和景点详情同步联动；
- `TravelInformation.tsx` 与 `SourceList.tsx`：抵达、准备、变化提示、核验日期和官方来源；
- 浏览器逐页验证三地路径、点位数量、路线类型、页面标题和支线交互。

## 阶段 6：内容、来源与体验收口

**状态：已完成**

目标是将研究内容可靠地转化为可发布页面。

主要工作：

- 校对所有地名、历史事实、开放信息、预约规则、交通和路线；
- 检查每张正式图片的来源记录、使用条件、替代文本和裁切效果；
- 检查网站没有实践报告化叙事、无依据统计和对目的地发展的价值判断；
- 优化信息密度、加载性能、图片尺寸、弱网体验和无障碍访问；
- 补齐页面标题、描述、分享信息、站点图标和必要的搜索引擎元信息。

退出条件：

- 发布内容能追溯到资料或明确标记为暂缺图片；
- 关键任务不依赖颜色、鼠标悬停或大屏幕才能完成；
- 页面没有明显重叠、溢出、布局跳动、失效链接或不可操作控件；
- 构建产物中不包含开发调试内容和无授权素材。

实际产物：

- 发布页面只使用 `PlaceholderImage` 生成的项目自有占位状态，研究附件未进入 `public/` 或页面代码；
- 官网候选图片继续保留在 `sources/images/IMAGE_LEDGER.csv` 的 `permission-needed` 状态；
- 页面支持键盘按钮语义、触控目标、减少动态效果偏好、移动菜单和表格移动重排；
- 对代码和文案完成“河南误命名、实践报告叙事、发展程度评价”扫描，命中仅存在于约束说明中。

## 阶段 7：部署验证与交付

**状态：已完成**

目标是交付可以稳定部署和后续维护的纯静态网站。

主要工作：

- 生成生产构建并使用本地静态服务器验证；
- 按桌面和移动端代表性尺寸进行截图与交互检查；
- 验证直接访问详情路径、刷新、资源路径和自定义域名场景；
- 准备 Cloudflare Pages 构建设置、必要的路由规则和部署说明；
- 编写内容更新、图片替换、来源维护和常见故障说明；
- 汇总仍需用户决策的问题及其影响。

退出条件：

- 生产构建无错误，可作为无后端静态站运行；
- 首页和三个详情体验在桌面与手机上通过功能和视觉检查；
- Cloudflare 部署步骤可由项目文档重复执行；
- 所有已知限制、暂缺图片和后续维护事项均有记录。

实际产物：

- `docs/DEPLOYMENT.md`：Cloudflare Pages 设置、直达路径检查、内容维护、图片替换和地图边界；
- 生产构建包含 `dist/_redirects` 与 `dist/data/hebei.geojson`；
- 使用生产预览验证首页和三个详情路径均可直接访问与刷新；
- 2026-08-27 在 1440×900 与 390×844 视口完成页面结构、交互、溢出和控制台检查，未发现阻碍主要流程的问题。

## 阶段 8：真实地图与全站强交互升级

**状态：已完成**

本阶段响应第二轮体验要求，在不放宽来源与图片授权约束的前提下，提升地图信息量、路线表达和所有主要操作的反馈。

主要成果：

- 河北地图点击后以目的地为中心聚焦，标记脉冲并在点位旁弹出带多图轮播、摘要、用时和详情入口的信息气泡；
- 三地详情导览增加入口、服务设施、停车、地铁站、水库方向、步道和峰顶等经过资料核验的空间信息；
- 新增石家庄至河北博物院、石家庄至西柏坡、易县至狼牙山的真实交通关系图，交通节点可点击；
- 已核验步行段持续显示沿路线运动的足迹；地铁、旅游公交和自驾路线显示对应运动图标；未获核验的摆渡车不制作；
- 首页气泡、目的地头图和每个内部景点均使用可自动轮播、前后切换、圆点选择和暂停恢复的多图组件；
- 悬停、按压、键盘焦点、选中、进度、弹出和加载状态形成统一反馈，并尊重 `prefers-reduced-motion`；
- `sources/research/interaction-upgrade/map-and-ui-research.md` 记录地图重绘依据、交互参考和发布边界。

验收证据：

- 2026-08-27 通过 `pnpm lint`、`pnpm typecheck` 和 `pnpm build`；
- 生产预览逐页验证首页和三个详情路径可直接访问、刷新且控制台无错误；
- 1440×900 与 390×844 视口均完成截图和交互检查，四个页面无页面级横向溢出；
- 三个详情页分别显示地铁 + 步行、旅游公交、自驾交通动画，三类导览底图均显示实际设施信息。

## 阶段 9：地图交互与来源细化

**状态：已完成**

本阶段处理地图选择状态、缩放反馈、步行足迹、交通图标比例和逐条来源入口等细节问题。

主要成果：

- 修改前版本已提交为 `6ad0957`，并建立标签 `backup-before-detail-optimization-20260828`；
- 河北总览地图使用同一原子状态驱动聚焦点、气泡和目的地摘要，避免快速切换时继续显示旧景区；
- 景区点击采用 2.25 倍聚焦，点击地图空白恢复全省比例；
- 徒步路线改为随每段方向旋转、左右脚交替且每枚独立错峰显隐的足迹序列；
- 交通工具图标按画布比例反向校正，避免圆形底座和图标被压成椭圆；
- 每条抵达方式、交通关系图和景区导览图均就地提供具体原始资料链接；
- 首页移除“三地怎么选”，页脚在首页改为真正的“回到顶部”操作。

退出条件：

- 连续点击三个景区时，标记、聚焦、气泡与右侧摘要始终一致；
- 点击空白区域后气泡关闭、地图恢复原比例；
- 三个详情页的交通工具与足迹在桌面、手机上不变形且方向正确；
- 逐条来源链接均存在，首页无已删除比较区，页脚操作有效；
- 通过 lint、类型检查、构建和桌面/手机浏览器验收。

验收证据：

- 2026-08-28 使用 1440×900 与 390×844 视口验证河北博物院、西柏坡、狼牙山标记选择；聚焦、气泡、摘要和 `aria-pressed` 一致；
- 点击行政区空白后气泡数量归零，桌面与手机计算缩放均恢复为 1；
- 交通图标圆形底座桌面宽高比约 1.04、手机约 0.99，未再出现明显椭圆变形；
- 三个详情页各显示 3 条抵达方式原始网页入口，导览与交通图也显示原始资料入口；
- 四个页面未出现横向溢出，浏览器控制台无错误或警告。

## 阶段 10：V2 资料与回归基线

**状态：已完成（2026-08-29）**

目标是在不修改首页的前提下，为冉庄详情和四地规划建立可追溯的事实基础与视觉回归基线。

主要工作：

- 保存当前线上首页在桌面、平板和手机尺寸下的截图、主要文本与交互状态，作为“首页不动”的回归基线；
- 调研冉庄地道战博物馆的官方名称、位置、核心看点、内部路线、开放与预约、抵达方式、配套和行前准备；
- 重新核验四地的开放时间、停止入场时间、常规闭馆日、预约要求和例外说明；
- 调研四地两两之间共六组交通关系，分别判断自驾和公共交通资料是否足够可靠；
- 调研适合作为午餐停靠和住宿落点的区域，不在无稳定来源时收录具体商家；
- 为冉庄候选图片建立来源与使用条件台账，未获授权时继续使用占位图。

退出条件：

- 四地开放信息都有具体页面来源和核验日期，冲突项被明确记录；
- 冉庄资料足以生成与现有三页同等完整度的详情页；
- 六组自驾关系均有依据，公共交通不可核验的边被明确标记为不可自动规划；
- 午餐与住宿建议能解释“推荐哪个区域、为什么”，但不包含无依据商家；
- 首页回归基线可用于后续自动或人工对照。

计划产物：

- `sources/research/ranzhuang/official-guide.md`；
- `sources/research/multi-destination/opening-hours.md`；
- `sources/research/multi-destination/connection-matrix.md`；
- `sources/research/multi-destination/meal-and-stay-bases.md`；
- 更新 `sources/reference.txt` 与 `sources/images/IMAGE_LEDGER.csv`。

实际产物已全部落盘；首页基线另保存于 `sources/baseline/v1-home/`，包含三档 DOM/几何记录与 1440×900、768×1024、390×844 截图。研究确认正式名称为“冉庄地道战纪念馆”，并完成四地开放冲突、六组自驾关系、公共交通边界及区域级餐宿落点记录。

## 阶段 11：四地数据模型与首页隔离

**状态：已完成（2026-08-29）**

目标是让四地详情、比较和规划由统一数据驱动，同时保证目的地数量增加不会改变首页。

主要工作：

- 将 `DestinationId` 增加 `ranzhuang-tunnel`；
- 增加结构化 `OpeningSchedule`，从自由文本中拆出开放、闭馆、停止入场、预约、例外和来源；
- 建立地点间 `ConnectionEdge`、午餐区域、住宿落点和规划约束的数据类型；
- 将完整的 `destinations` 与首页专用 `homeDestinations` 分离；
- 建立纯函数形式的日期检查、耗时汇总、访问顺序评估和日程分天逻辑；
- 为算法准备小规模单元测试或确定性数据测试，不把路线规则藏进 UI 组件。

退出条件：

- 增加第四个目的地后，首页只接收原有三个地点；
- 四个详情页可使用同一开放时间组件；
- 任意地点组合都能由类型完整的数据描述，不使用组件内硬编码地名；
- 缺失的公共交通边返回明确的“无法可靠规划”状态，而不是默认估算。

实际产物：`src/types/planner.ts`、`src/data/planner.ts`、`src/lib/itinerary.ts` 与对应测试。公交边已具备方向和运营日结构；完整四地数据与首页专用三地数据保持隔离。

## 阶段 12：冉庄详情与四地开放时间

**状态：已完成（2026-08-29）**

目标是补齐第四个独立景区体验，并让游客在每个详情页快速确认可参观时段。

主要工作：

- 按资料选择适合冉庄的真实导览底图表达、内部点位和路线；
- 实现冉庄的头图轮播、内部景点联动、抵达关系图、准备事项和来源；
- 在四个详情页增加统一的开放时间模块，突出“今天通常是否开放”但不替代官方临时通知；
- 在四个详情页增加“加入对比与行程”入口，并把当前地点写入规划页 URL；
- 对来源冲突、临时调整和缺图分别提供稳定降级状态。

退出条件：

- 冉庄详情能回答看点、位置、差异、抵达、内部游览、准备和适合人群；
- 四页均显示日常开闭馆、停止入场、预约、例外、来源和核验日期；
- 详情页进入规划页时当前地点已勾选，返回详情不会丢失必要上下文；
- 原有三个详情页除新增模块和入口外没有功能回归。

实际产物：冉庄完整详情数据、`OpeningHoursPanel.tsx`、四页规划入口及村落 / 地道导览样式。浏览器核验四页均显示开放、预约、例外、来源与核验日期，冉庄标题使用正式名称。

## 阶段 13：四地多选与横向比较

**状态：已完成（2026-08-29）**

目标是让用户同时看到任意几个地点的信息，并在有限信息密度下完成选择。

主要工作：

- 新建 `/planner` 页面和四地复选控件；
- 实现 0、1、2 至 4 个选择状态以及 URL 参数持久化；
- 实现开放、主题、空间、用时、强度、交通、预约、午餐、住宿和限制的简要比较；
- 桌面端采用稳定分列，移动端改为纵向摘要，不使用只能一次显示一地的轮播；
- 比较列提供详情入口，并保留当前选择参数。

退出条件：

- 所有 16 种选择集合都能正确渲染，取消勾选后没有旧数据残留；
- 选择状态刷新后可以恢复，也能通过链接带入；
- 最长地名、开放说明和限制信息在桌面、平板、手机均不溢出；
- 比较内容保持简要，详细内部路线仍回到景区详情页查看。

实际产物：`src/pages/PlannerPage.tsx` 与规划页响应式样式。选择、日期、方式、天数、节奏和出发区域均持久化到 URL；浏览器已核验取消选择后比较、路线和计数同步更新。

## 阶段 14：串联路线与逐日行程生成

**状态：已完成（2026-08-29）**

目标是把 2 至 4 个已选地点转化为包含通勤、参观、午餐、准备和住宿的可执行建议。

主要工作：

- 增加日期、交通方式、可用天数、行程节奏和出发落点控件；
- 枚举最多四个地点的访问顺序，按闭馆冲突、停止入场、通勤、每日总时长和游览强度排序；
- 在一日不可行时明确要求增加天数，不压缩最低合理参观时间；
- 生成按天时间轴，包含出发、通勤、参观、午餐窗口、缓冲、住宿或返程；
- 汇总每日与全程的参观时间、通勤时间、缓冲和准备事项；
- 为每段交通和易变化信息提供原始来源与核验入口；
- 在资料允许时提供“通勤更短”和“体力更轻松”两种排序，无法形成可靠备选时只给一个方案。

退出条件：

- 任意 2、3、4 地组合在自驾模式下都能给出结果或明确不可行原因；
- 公共交通模式只使用已核验关系，缺边时给出可操作的改期、拆分或换交通方式建议；
- 常规闭馆冲突、晚于停止入场、超出每日时长等情况都有明确反馈；
- 每个日程都包含午餐、住宿/返程和准备信息，不只是地点与连线；
- 算法输出具有确定性，相同输入不会无理由改变访问顺序。

实际产物：确定性枚举排程、真实 OpenStreetMap 底图关系视图、逐日时间轴与直接来源入口。每一天均包含一次午餐、返程或住宿落点和准备事项；无可靠公共交通链时拒绝自动补齐。

## 阶段 15：全组合验收与部署

**状态：进行中（2026-08-29）**

目标是验证四地内容和路线生成在静态部署条件下可靠工作，并确认首页零回归。

主要工作：

- 运行 lint、类型检查、构建和路线规划数据测试；
- 覆盖 6 个两地组合、4 个三地组合、1 个四地组合以及两种交通方式；
- 检查周一、正常开放日、单日不足、多日规划和公共交通缺边等边界场景；
- 在 1440×900、768×1024、390×844 下验证比较、表单、时间轴、地图和来源链接；
- 将首页与阶段 10 基线对照，确认点位、摘要、尺寸、文案和交互未变化；
- 验证四个详情路径和 `/planner` 可直接访问、刷新并通过 Cloudflare 回退；
- 更新部署、内容维护和核验日期说明后提交发布。

当前证据：`pnpm lint`、`pnpm typecheck`、`pnpm build` 已通过；两组 Vitest 共 26 项通过，覆盖 6 个两地、4 个三地、1 个四地组合，以及闭馆、非法日期、单日不足、公交方向、运营日、跨日落脚连续性、午餐和末日返程。规划页三档截图保存于 `sources/baseline/v2-planner/`；三档均无横向溢出，桌面 / 平板 / 手机分别为四列 / 两列 / 单列。首页仍为 3 个地图点、3 个备用按钮和原 4 项导航，无规划页正文回流。

退出条件：

- 首页基线无非预期变化；
- 四个详情页和规划页没有空白、横向溢出、遮挡、失效主操作或控制台错误；
- 所有组合不会崩溃，不会引用未选择地点或无来源交通数据；
- 自定义域名部署后直达路径、刷新和核心交互均正常。

## 最终验收标准

- 网站第一身份是好用、有吸引力、信息完整的旅游宣传与规划工具，不是实践活动报告。
- 用户可以从河北总览进入原有三个目的地，并完成查看看点、规划抵达和规划内部游览的主要流程。
- 用户可以从详情页进入独立规划页，勾选四个目的地中的任意若干项，同时比较并生成可执行的多地行程。
- 四个目的地的差异表达服务于游客决策，不以发展差距构建叙事。
- 图片可以暂时占位，其他核心可视化信息不得占位。
- 事实和正式图片来源可追溯，易变化信息有核验日期或外部确认入口。
- 网站无后端依赖，可构建、可测试、可部署到 Cloudflare。
- 桌面和移动端没有阻碍主要流程的显示、交互、性能或无障碍问题。

## Goal 期间的记录方式

- 每个阶段开始和完成时更新“状态”，并在阶段下补充实际产物路径。
- 不影响推进的问题集中记录，优先采用可配置实现，待 Goal 完成或真正阻塞时统一请用户决策。
- 发现新约束时，先判断是否与 `CONSTRAINTS.md` 冲突；涉及产品定位或范围变化的内容不得自行吸收，必须留待用户确认。
